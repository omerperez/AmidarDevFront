import React from "react";

export default function SectionTitle({ title }) {
  return (
    <div className="section-title-pos">
      <b className="section-title-22">{title}</b>
    </div>
  );
}
